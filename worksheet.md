# 六段填寫表

姓名：__________　　`/model` 選單分組：A（有 Opus）／ B（只有 Sonnet 與 Haiku）

> 填寫說明：標【事實】的欄位是這個 repo 裡查得到的紀錄，可逐條回去驗證。
> 標【我的判斷】的欄位是 AI 代填的意見，請你自己複核後改成你的說法。
> 留白的欄位有寫明留白原因，不要當成漏填。

---

## L0 建骨架｜Capability

一句話宣告（AI 能夠在 ＿情境＿ 下，使用 ＿知識＿，遵守 ＿規則＿，完成 ＿工作＿）：

> 【事實，出自 `CLAUDE.md`】AI 能夠在維護官網客服知識頁的情境下，使用 `content/` 的內容與 `design/brief.md` 的版型規範，遵守 `CLAUDE.md` 的規則，完成靜態頁面的生成與更新。

AI 做：【事實】讀 `content/` 生成頁面、套用版型、指出 `content/` 的缺漏與矛盾。

人做：【事實】決定政策內容、核准對外文案、決定某個品項是否上架、處理客訴。

**驗收句**：如果我現在把資料夾交給別人，他知道要往哪裡放東西嗎？　■ 是　□ 否

【我的判斷】結構與規則都寫在檔案裡（`CLAUDE.md` 分層、`design/brief.md` 版型契約、`SKILL.md` 執行入口），接手的人知道東西該放哪一層。但 `content/` 目前是空的，他會知道位置，不會知道內容長什麼樣。

---

## L1 內容層｜Knowledge

我把 `source/` 蒸餾成哪四份？

- [ ] `content/faq.md`
- [ ] `content/policy.md`
- [ ] `content/products.md`
- [ ] `content/about.md`

我找到**已經過期**的一項資訊：

> 留白。原因：`source/` 至今只有 `.gitkeep`，起手包的六份原始資料從未進入本 repo，無法執行蒸餾。

我找到**互相矛盾**的一項資訊，我的處理方式是：

> 留白。原因同上。

我找到**不該上架**的一個品項：

> 留白。原因同上。

**驗收句**：`source/` 裡有一項資訊已經過期，你找到了嗎？　□ 是　■ 否

【事實】四個內容檔目前皆為 0 bytes。`scripts/validate.py` 在此狀態回傳離開碼 2，訊息為「目前是 L0 空殼」。

---

## L2 版型｜Context

`design/brief.md` 裡我寫了什麼（對象、語氣、品牌色、裝置優先序）：

【事實，出自 `design/brief.md` 第 0 節】

- **對象**：已經是客戶、而且正在遇到問題的人。次要讀者是尚未購買、想先確認政策的人。
- **語氣**：直述、負責、不推託。稱對方為「您」，自稱「我們」。不使用驚嘆號，不推銷。有壞消息先講結論再講原因。
- **品牌色**：中性藍為主色，定義在 `design/tokens.json`。已明確標記為「待確認的工作值，不是已核定的品牌色」。
- **裝置優先序**：行動優先。手機 > 桌機 > 列印 > 平板。列印排在平板之前，因為政策頁與權限對照表有實際列印需求。

**驗收句**：把 `design/brief.md` 藏起來再叫它做第五頁，樣式會不會跑掉？　■ 會　□ 不會

【事實】這一條不只是宣稱，是機器強制的。`scripts/validate.py` 會掃描產出的樣式表，`:root` token 區塊以外只要出現任何 `#RRGGBB` 或 `rgb()` 就擋下交付。抽掉 tokens.json，`build.py` 會直接失敗。

---

## L3 記憶｜Memory

值得被記住的三個決定（不是聊天記錄，是未來還會用到的）：

【事實，出自 `memory/decisions.md`，共 11 則，此處取三則】

1. **內容與版型分離，網站從 `content/` 長出來。** 內容寫死在 HTML 裡，每次更新都要重新理解整份版面，錯誤率高且無法驗證。
2. **色彩全部走 token，樣式表本體禁止色碼字面值。** 這是 L2 驗收句的具體實作，色碼寫死在別處就是樣式脫離契約的證據。
3. **缺資料時留白並回報，不使用佔位符。** 佔位符會隨一次疏忽直接上線，空白至少不會誤導使用者。

絕對不可以被記住的是：

【事實】`restricted/` 的內容。已由 `.claude/settings.json` 的 `permissions.deny` 在系統層封鎖讀寫。

**驗收句**：關掉 session 重開，它還記得你為什麼選了這個配色嗎？　■ 是　□ 否

【事實】理由寫在兩個地方：`design/brief.md` 0.3 節（為何是中性藍、為何標記為待確認），以及 `memory/decisions.md` 的對應條目。

---

## L4 規則｜Rule

軟規則（CLAUDE.md，模型會參考但可能違反）三條：

【事實，出自 `CLAUDE.md` 今日暫行規則】

1. 未經明確指示，不得生成 `site/` 底下的任何檔案。
2. 未經明確指示，不得讀取 `source/` 的內容。
3. 未經明確指示，不得填寫 `content/` 的任何內容。

硬規則（settings.json，系統直接擋）一條：

【事實】`"deny": ["Read(./restricted/**)"]`，連同 `Edit`、`Write`、`Bash(cat …)` 等共 12 條。

我實測「叫它做一件被 deny 的事」，看到的畫面是：

【事實】我要求做一個家電公司客服網站首頁。AI 沒有動手，回報說 `CLAUDE.md` 的暫行規則禁止生成 `site/` 底下的檔案，並指出 `.claude/settings.json` 把 `Edit(./site/**)` 放在 allow 清單，所以系統不會攔、也不會跳權限確認。等我明確授權後才執行。

**驗收句**：是誰擋下來的，模型還是系統？　■ 模型　□ 系統

【事實】這一次是模型。`restricted/` 才是系統擋。同一個 repo 裡兩種擋法同時存在，可直接對照。

---

## L5 交換測試

我拿到的是誰的資料夾：__________

我需要回頭問對方問題：____ 次

我做出來的第五頁，跟他前四頁像不像：　□ 幾乎一樣　□ 有落差　□ 完全不像

他哪一層寫得最好：

> 整段留白。原因：需要另一位學員的資料夾才能進行，本 session 沒有交換對象。

**這一段強制用 Sonnet 5。** 如果你的架構夠好，通才模型也做得對。要靠最強模型才做得對，代表你的架構在替模型補洞。

---

## 收束

我這件工作，五層裡最弱的一層是：

> 【我的判斷】L1 內容層。

因為：

> 【我的判斷】其他四層都有實體：L0 有完整目錄樹、L2 有 9KB 版型契約加機器強制、L3 有 11 則決策紀錄、L4 有軟硬兩種規則且可對照。只有 L1 是 0 bytes。整個架構蓋得很完整，但它要承載的知識一個字都沒有，等於蓋好了倉庫沒有貨。
>
> 更值得注意的是盤點表 20 列，維護者欄 20 個「未指定」，沒有任何一份文件記載誰負責維護什麼。這讓分層表與流程表都做不出來，因為兩者問的都是「誰」。

Day 2 我最想先補的是：

> 【我的判斷】把 `source/` 六份原始資料放進 repo，完成蒸餾。這一步不做，後面的搜尋、表單、多代理全都沒有東西可以接。
